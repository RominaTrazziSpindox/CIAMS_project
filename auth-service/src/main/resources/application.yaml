spring:
  application:
    name: auth-service
  data:
    mongodb:
      uri: mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}/${MONGO_DB:auth_db}
  security:
    jwt:
      secret: ${JWT_SECRET}
      expiration: 3600000